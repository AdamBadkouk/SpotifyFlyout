<ui:FluentWindow
    x:Class="SpotifyFlyoutWPF.SettingsWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:SpotifyFlyoutWPF"
    xmlns:pages="clr-namespace:SpotifyFlyoutWPF.Pages"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    mc:Ignorable="d"
    Height="700" MinHeight="300" MinWidth="750" Width="900"
    Title="SpotifyFlyout" Icon="/Resources/SpotifyFlyout.ico"
    TextOptions.TextRenderingMode="ClearType" TextOptions.TextFormattingMode="Ideal"
    FontFamily="{Binding FontFamily}"
	FlowDirection="{Binding FlowDirection}"
    Closing="SettingsWindow_Closing"
    xmlns:viewModels="clr-namespace:SpotifyFlyoutWPF.ViewModels"
    d:DataContext="{d:DesignInstance viewModels:UserSettings}"
    ui:Design.Background="{DynamicResource ApplicationBackgroundBrush}"
    ui:Design.Foreground="{DynamicResource TextFillColorPrimaryBrush}"
    ExtendsContentIntoTitleBar="True"
    WindowBackdropType="Mica"
    Loaded="SettingsWindow_Loaded">

	<Grid>
		<ui:NavigationView x:Name="RootNavigation"
                           PaneDisplayMode="Left"
                           TransitionDuration="0"
                           IsPaneToggleVisible="true"
                           IsPaneOpen="True"
                           Margin="0, 40, 0, 0"
						   FrameMargin="0, 0, 0, 0"
                           OpenPaneLength="250"
                           IsTopSeparatorVisible="false"
                           IsFooterSeparatorVisible="false">
			<ui:NavigationView.MenuItems>
				<ui:NavigationViewItem Content="Spotify Flyout" Icon="{ui:SymbolIcon VideoPlayPause24}" TargetPageType="{x:Type pages:SpotifyFlyoutPage}" />
                <ui:NavigationViewItem Content="Next Up Flyout" Icon="{ui:SymbolIcon MusicNote2Play20}" TargetPageType="{x:Type pages:NextUpPage}" />
				<ui:NavigationViewItem Content="Taskbar Widget" Icon="{ui:SymbolIcon TextboxAlignBottom24}" TargetPageType="{x:Type pages:TaskbarWidgetPage}" />
				<ui:NavigationViewItem Content="Taskbar Visualizer" Icon="{ui:SymbolIcon SoundWaveCircle24}" TargetPageType="{x:Type pages:VisualizerPage}" Click="NavigationViewItem_Click" />
				<ui:NavigationViewItem Content="{DynamicResource SystemSettingsTitle}" Icon="{ui:SymbolIcon Settings24}" TargetPageType="{x:Type pages:SystemPage}" />
			</ui:NavigationView.MenuItems>
			<ui:NavigationView.FooterMenuItems>
				<ui:NavigationViewItem Content="{DynamicResource AboutTitle}" Icon="{ui:SymbolIcon Info24}" TargetPageType="{x:Type pages:AboutPage}" />
			</ui:NavigationView.FooterMenuItems>
		</ui:NavigationView>

		<ui:TitleBar
            x:Name="TitleBar"
            Title="SpotifyFlyout"
            Grid.Row="0">
			<ui:TitleBar.Icon>
				<ui:ImageIcon Source="/Resources/SpotifyFlyout.ico" />
			</ui:TitleBar.Icon>
		</ui:TitleBar>
	</Grid>
</ui:FluentWindow>