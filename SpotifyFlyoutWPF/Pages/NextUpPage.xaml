<Page x:Class="SpotifyFlyoutWPF.Pages.NextUpPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
      xmlns:controls="clr-namespace:MicaWPF.Controls;assembly=MicaWPF"
      xmlns:viewModels="clr-namespace:SpotifyFlyoutWPF.ViewModels"
      mc:Ignorable="d"
      d:DesignHeight="800" d:DesignWidth="800"
      Title="NextUpPage"
      d:DataContext="{d:DesignInstance viewModels:UserSettings}"
      Foreground="{DynamicResource TextFillColorPrimaryBrush}"
      d:Background="White">

    <StackPanel MaxWidth="1000" Margin="16">
        <TextBlock Text="{DynamicResource NextUpCustomizationTitle}" FontSize="24" FontWeight="SemiBold" Margin="0,0,0,8" />
        <TextBlock Text="{DynamicResource NextUpDescription}" FontSize="14" Opacity="0.6" Margin="0,0,0,20" TextWrapping="Wrap" />

        <ui:CardControl Icon="{ui:SymbolIcon MusicNote2Play20}" Margin="0,0,0,12">
            <ui:CardControl.Header>
                <StackPanel Orientation="Vertical">
                    <TextBlock Text="{DynamicResource EnableNextUpTitle}" FontSize="14" FontWeight="Regular" />
                    <TextBlock Text="{DynamicResource EnableNextUpDescription}" FontSize="12" Opacity="0.5" />
                </StackPanel>
            </ui:CardControl.Header>
            <controls:ToggleSwitch Name="NextUpSwitch" IsChecked="{Binding NextUpEnabled, Mode=TwoWay}" />
        </ui:CardControl>

        <ui:CardControl Icon="{ui:SymbolIcon Blur24}" Margin="0,0,0,3">
            <ui:CardControl.Header>
                <StackPanel Orientation="Vertical">
                    <TextBlock Text="{DynamicResource AcrylicWindowTitle}" FontSize="14" FontWeight="Regular" />
                    <TextBlock Text="{DynamicResource AcrylicWindowDescription}" FontSize="12" Opacity="0.5" />
                </StackPanel>
            </ui:CardControl.Header>
            <controls:ToggleSwitch Name="NextUpAcrylicWindowSwitch" IsChecked="{Binding NextUpAcrylicWindowEnabled, Mode=TwoWay}" />
        </ui:CardControl>

        <ui:CardControl Icon="{ui:SymbolIcon Timer24}" Margin="0,0,0,12">
            <ui:CardControl.Header>
                <StackPanel Orientation="Vertical">
                    <TextBlock Text="{DynamicResource NextUpStayDurationTitle}" FontSize="14" FontWeight="Regular" />
                    <TextBlock Text="{DynamicResource NextUpStayDurationDescription}" FontSize="12" Opacity="0.5" />
                </StackPanel>
            </ui:CardControl.Header>
            <StackPanel Orientation="Horizontal">
                <ui:TextBox Name="NextUpDurationTextBox" Width="60" ClearButtonEnabled="False" Text="{Binding NextUpDurationText, Mode=TwoWay}" />
                <TextBlock Text="{DynamicResource MillisecondsUnit}" FontSize="14" Margin="10,0,0,0" VerticalAlignment="Center" />
            </StackPanel>
        </ui:CardControl>
    </StackPanel>
</Page>
