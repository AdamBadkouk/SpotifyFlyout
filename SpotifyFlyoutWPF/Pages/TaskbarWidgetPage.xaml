<Page x:Class="SpotifyFlyoutWPF.Pages.TaskbarWidgetPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
      xmlns:controls="clr-namespace:MicaWPF.Controls;assembly=MicaWPF"
      xmlns:viewModels="clr-namespace:SpotifyFlyoutWPF.ViewModels"
      mc:Ignorable="d"
      d:DesignHeight="1200" d:DesignWidth="800"
      Title="TaskbarWidgetPage"
      d:DataContext="{d:DesignInstance viewModels:UserSettings}"
      Foreground="{DynamicResource TextFillColorPrimaryBrush}"
      d:Background="White"
      Loaded="Page_Loaded">

    <StackPanel MaxWidth="1000" Margin="16">
        <TextBlock Text="{DynamicResource TaskbarWidgetCustomizationTitle}" FontSize="24" FontWeight="SemiBold" Margin="0,0,0,8" />
        <TextBlock Text="{DynamicResource TaskbarWidgetDescription}" FontSize="14" Opacity="0.6" Margin="0,0,0,20" TextWrapping="Wrap" />

        <ui:CardControl Icon="{ui:SymbolIcon TextboxAlignBottom24}" Margin="0,0,0,12">
            <ui:CardControl.Header>
                <StackPanel Orientation="Vertical">
                    <TextBlock Text="{DynamicResource TaskbarWidgetEnabledTitle}" FontSize="14" FontWeight="Regular" />
                    <TextBlock Text="{DynamicResource TaskbarWidgetEnabledDescription}" FontSize="12" Opacity="0.5" />
                </StackPanel>
            </ui:CardControl.Header>
            <controls:ToggleSwitch IsChecked="{Binding TaskbarWidgetEnabled, Mode=TwoWay}" />
        </ui:CardControl>
        
        <ui:InfoBar Title="{DynamicResource TaskbarWidgetVisualIssuesWarning}" IsOpen="True" Severity="Informational" IsClosable="False" Margin="0,0,0,12" />

        <ui:CardControl Icon="{ui:SymbolIcon ArrowMove24}" Margin="0,0,0,3">
            <ui:CardControl.Header>
                <TextBlock Text="{DynamicResource TaskbarWidgetPosition}" FontSize="14" FontWeight="Regular" />
            </ui:CardControl.Header>
            <ComboBox SelectedIndex="{Binding TaskbarWidgetPosition}">
                <ComboBoxItem Content="{DynamicResource PositionBottomLeft}" />
                <ComboBoxItem Content="{DynamicResource PositionBottomCenter}" />
                <ComboBoxItem Content="{DynamicResource PositionBottomRight}" />
            </ComboBox>
        </ui:CardControl>
        
        <ui:CardControl Icon="{ui:SymbolIcon ProjectionScreen24}" Margin="0,0,0,3">
            <ui:CardControl.Header>
                <TextBlock Text="{DynamicResource TaskbarWidgetSelectedMonitorTitle}" FontSize="14" FontWeight="Regular" />
            </ui:CardControl.Header>
            <ComboBox x:Name="TaskbarWidgetSelectedMonitorComboBox" SelectedIndex="{Binding TaskbarWidgetSelectedMonitor, Mode=TwoWay}" />
        </ui:CardControl>

        <ui:CardExpander Icon="{ui:SymbolIcon Wrench24}" ContentPadding="6" Margin="0,0,0,3" IsExpanded="False">
            <ui:CardExpander.Header>
                <StackPanel Orientation="Vertical">
                    <TextBlock Text="{DynamicResource TaskbarWidgetPaddingSettingsTitle}" FontSize="14" FontWeight="Regular" />
                    <TextBlock Text="{DynamicResource TaskbarWidgetPaddingSettingsDescription}" FontSize="12" Opacity="0.5" />
                </StackPanel>
            </ui:CardExpander.Header>
            <StackPanel>
                <ui:CardControl Icon="{ui:SymbolIcon PaddingRight24}" Margin="0,0,0,3">
                    <ui:CardControl.Header>
                        <StackPanel Orientation="Vertical">
                            <TextBlock Text="{DynamicResource TaskbarWidgetPaddingTitle}" FontSize="14" FontWeight="Regular" />
                            <TextBlock Text="{DynamicResource TaskbarWidgetPaddingDescription}" FontSize="12" Opacity="0.5" />
                        </StackPanel>
                    </ui:CardControl.Header>
                    <controls:ToggleSwitch IsChecked="{Binding TaskbarWidgetPadding, Mode=TwoWay}">
                        <controls:ToggleSwitch.Style>
                            <Style TargetType="controls:ToggleSwitch" BasedOn="{StaticResource {x:Type controls:ToggleSwitch}}">
                                <Setter Property="IsEnabled" Value="False" />
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding TaskbarWidgetPosition}" Value="0">
                                        <Setter Property="IsEnabled" Value="True" />
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding TaskbarWidgetPosition}" Value="2">
                                        <Setter Property="IsEnabled" Value="True" />
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </controls:ToggleSwitch.Style>
                    </controls:ToggleSwitch>
                </ui:CardControl>
                <ui:CardControl Icon="{ui:SymbolIcon ArrowFit20}" Margin="0,0,0,0">
                    <ui:CardControl.Header>
                        <StackPanel Orientation="Vertical">
                            <TextBlock Text="{DynamicResource TaskbarWidgetManualPaddingTitle}" FontSize="14" FontWeight="Regular" />
                            <TextBlock Text="{DynamicResource TaskbarWidgetManualPaddingDescription}" FontSize="12" Opacity="0.5" />
                        </StackPanel>
                    </ui:CardControl.Header>
                    <StackPanel Orientation="Horizontal">
                        <ui:TextBox Width="60" ClearButtonEnabled="False" Text="{Binding TaskbarWidgetManualPaddingText, Mode=TwoWay}" />
                        <TextBlock Text="{DynamicResource PixelsUnit}" FontSize="14" Margin="10,0,0,0" VerticalAlignment="Center" />
                    </StackPanel>
                </ui:CardControl>
            </StackPanel>
        </ui:CardExpander>

        <ui:CardControl Icon="{ui:SymbolIcon ColorBackground24}" Margin="0,0,0,3">
            <ui:CardControl.Header>
                <StackPanel Orientation="Vertical">
                    <TextBlock Text="{DynamicResource TaskbarWidgetBackgroundBlurTitle}" FontSize="14" FontWeight="Regular" />
                    <TextBlock Text="{DynamicResource TaskbarWidgetBackgroundBlurDescription}" FontSize="12" Opacity="0.5" />
                </StackPanel>
            </ui:CardControl.Header>
            <controls:ToggleSwitch IsChecked="{Binding TaskbarWidgetBackgroundBlur, Mode=TwoWay}" />
        </ui:CardControl>
        
        <ui:CardControl Icon="{ui:SymbolIcon EyeOff24}" Margin="0,0,0,3">
            <ui:CardControl.Header>
                <StackPanel Orientation="Vertical">
                    <TextBlock Text="{DynamicResource TaskbarWidgetHideCompletelyTitle}" FontSize="14" FontWeight="Regular" />
                    <TextBlock Text="{DynamicResource TaskbarWidgetHideCompletelyDescription}" FontSize="12" Opacity="0.5" />
                </StackPanel>
            </ui:CardControl.Header>
            <controls:ToggleSwitch IsChecked="{Binding TaskbarWidgetHideCompletely, Mode=TwoWay}" />
        </ui:CardControl>
        
        <ui:CardControl Icon="{ui:SymbolIcon EyeLines24}" Margin="0,0,0,3">
            <ui:CardControl.Header>
                <StackPanel Orientation="Vertical">
                    <TextBlock Text="{DynamicResource TaskbarWidgetAutoHideTitle}" FontSize="14" FontWeight="Regular" />
                    <TextBlock Text="{DynamicResource TaskbarWidgetAutoHideDescription}" FontSize="12" Opacity="0.5" />
                </StackPanel>
            </ui:CardControl.Header>
            <controls:ToggleSwitch IsChecked="{Binding TaskbarWidgetAutoHide, Mode=TwoWay}" />
        </ui:CardControl>
        
        <ui:CardControl Icon="{ui:SymbolIcon VideoPlayPause24}" Margin="0,0,0,3">
            <ui:CardControl.Header>
                <StackPanel Orientation="Vertical">
                    <TextBlock Text="{DynamicResource TaskbarWidgetControlsTitle}" FontSize="14" FontWeight="Regular" />
                    <TextBlock Text="{DynamicResource TaskbarWidgetControlsDescription}" FontSize="12" Opacity="0.5" />
                </StackPanel>
            </ui:CardControl.Header>
            <controls:ToggleSwitch IsChecked="{Binding TaskbarWidgetControlsEnabled, Mode=TwoWay}" />
        </ui:CardControl>
        
        <ui:CardControl Icon="{ui:SymbolIcon ArrowMove24}" Margin="0,0,0,3">
            <ui:CardControl.Header>
                <StackPanel Orientation="Vertical">
                    <TextBlock Text="{DynamicResource TaskbarWidgetControlsPositionTitle}" FontSize="14" FontWeight="Regular" />
                    <TextBlock Text="{DynamicResource TaskbarWidgetControlsPositionDescription}" FontSize="12" Opacity="0.5" />
                </StackPanel>
            </ui:CardControl.Header>
            <ComboBox SelectedIndex="{Binding TaskbarWidgetControlsPosition}">
                <ComboBoxItem Content="{DynamicResource PositionLeft}" />
                <ComboBoxItem Content="{DynamicResource PositionRight}" />
            </ComboBox>
        </ui:CardControl>
        
        <ui:CardControl Icon="{ui:SymbolIcon Wand24}" Margin="0,0,0,12">
            <ui:CardControl.Header>
                <StackPanel Orientation="Vertical">
                    <TextBlock Text="{DynamicResource TaskbarWidgetAnimatedTitle}" FontSize="14" FontWeight="Regular" />
                    <TextBlock Text="{DynamicResource TaskbarWidgetAnimatedDescription}" FontSize="12" Opacity="0.5" />
                </StackPanel>
            </ui:CardControl.Header>
            <controls:ToggleSwitch IsChecked="{Binding TaskbarWidgetAnimated, Mode=TwoWay}" />
        </ui:CardControl>
    </StackPanel>
</Page>
