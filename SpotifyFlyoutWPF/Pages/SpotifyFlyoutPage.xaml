<Page x:Class="SpotifyFlyoutWPF.Pages.SpotifyFlyoutPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
      xmlns:controls="clr-namespace:MicaWPF.Controls;assembly=MicaWPF"
      xmlns:viewModels="clr-namespace:SpotifyFlyoutWPF.ViewModels"
      mc:Ignorable="d"
      d:DesignHeight="1600" d:DesignWidth="800"
      Title="SpotifyFlyoutPage"
      d:DataContext="{d:DesignInstance viewModels:UserSettings}"
      Foreground="{DynamicResource TextFillColorPrimaryBrush}"
      d:Background="White">

    <StackPanel MaxWidth="1000" Margin="16">
        <TextBlock Text="{DynamicResource MediaFlyoutTitle}" FontSize="24" FontWeight="SemiBold" Margin="0,0,0,8" />
        <TextBlock Text="{DynamicResource MediaFlyoutDescription}" FontSize="14" Opacity="0.6" Margin="0,0,0,20" TextWrapping="Wrap" />

        <ui:CardControl Icon="{ui:SymbolIcon VideoPlayPause24}" Margin="0,0,0,12">
            <ui:CardControl.Header>
                <StackPanel Orientation="Vertical">
                    <TextBlock Text="{DynamicResource TextBlockText1}" FontSize="14" FontWeight="Regular" />
                    <TextBlock Text="{DynamicResource TextBlockText3}" FontSize="12" Opacity="0.5" />
                </StackPanel>
            </ui:CardControl.Header>
            <controls:ToggleSwitch Name="MediaFlyoutEnabledSwitch" IsChecked="{Binding MediaFlyoutEnabled, Mode=TwoWay}" />
        </ui:CardControl>

        <ui:CardControl Icon="{ui:SymbolIcon ColorBackground24}" Margin="0,0,0,3">
            <ui:CardControl.Header>
                <StackPanel Orientation="Vertical">
                    <TextBlock Text="{DynamicResource BackgroundBlurTitle}" FontSize="14" FontWeight="Regular" />
                    <TextBlock Text="{DynamicResource TextBlockText2}" FontSize="12" Opacity="0.5" />
                </StackPanel>
            </ui:CardControl.Header>
            <ComboBox Name="BackgroundComboBox" SelectedIndex="{Binding MediaFlyoutBackgroundBlur}">
                <ComboBoxItem Content="{DynamicResource BackgroundBlurOption1}" />
                <ComboBoxItem Content="{DynamicResource BackgroundBlurOption2}" />
                <ComboBoxItem Content="{DynamicResource BackgroundBlurOption3}" />
                <ComboBoxItem Content="{DynamicResource BackgroundBlurOption4}" />
            </ComboBox>
        </ui:CardControl>

        <ui:CardControl Icon="{ui:SymbolIcon Blur24}" Margin="0,0,0,3">
            <ui:CardControl.Header>
                <StackPanel Orientation="Vertical">
                    <TextBlock Text="{DynamicResource AcrylicWindowTitle}" FontSize="14" FontWeight="Regular" />
                    <TextBlock Text="{DynamicResource AcrylicWindowDescription}" FontSize="12" Opacity="0.5" />
                </StackPanel>
            </ui:CardControl.Header>
            <controls:ToggleSwitch Name="AcrylicWindowSwitch" IsChecked="{Binding MediaFlyoutAcrylicWindowEnabled, Mode=TwoWay}" />
        </ui:CardControl>

        <ui:CardControl Icon="{ui:SymbolIcon ArrowAutofitWidth24}" Margin="0,0,0,3">
            <ui:CardControl.Header>
                <StackPanel Orientation="Vertical">
                    <TextBlock Text="{DynamicResource CompactLayoutTitle}" FontSize="14" FontWeight="Regular" />
                    <TextBlock Text="{DynamicResource CompactLayoutDescription}" FontSize="12" Opacity="0.5" />
                </StackPanel>
            </ui:CardControl.Header>
            <controls:ToggleSwitch Name="LayoutSwitch" IsChecked="{Binding CompactLayout, Mode=TwoWay}" />
        </ui:CardControl>

        <ui:CardControl Icon="{ui:SymbolIcon ArrowMove24}" Margin="0,0,0,3">
            <ui:CardControl.Header>
                <TextBlock Text="{DynamicResource FlyoutPositionTitle}" FontSize="14" FontWeight="Regular" />
            </ui:CardControl.Header>
            <ComboBox Name="PositionComboBox" SelectedIndex="{Binding Position, Mode=TwoWay}">
                <ComboBoxItem Content="{DynamicResource PositionBottomLeft}" />
                <ComboBoxItem Content="{DynamicResource PositionBottomCenter}" />
                <ComboBoxItem Content="{DynamicResource PositionBottomRight}" />
                <ComboBoxItem Content="{DynamicResource PositionTopLeft}" />
                <ComboBoxItem Content="{DynamicResource PositionTopCenter}" />
                <ComboBoxItem Content="{DynamicResource PositionTopRight}" />
            </ComboBox>
        </ui:CardControl>

        <ui:CardControl Icon="{ui:SymbolIcon Timer24}" Margin="0,0,0,3">
            <ui:CardControl.Header>
                <StackPanel Orientation="Vertical">
                    <TextBlock Text="{DynamicResource FlyoutStayDurationTitle}" FontSize="14" FontWeight="Regular" />
                    <TextBlock Text="{DynamicResource FlyoutStayDurationDescription}" FontSize="12" Opacity="0.5" />
                </StackPanel>
            </ui:CardControl.Header>
            <StackPanel Orientation="Horizontal">
                <ui:TextBox Name="DurationTextBox" Width="60" ClearButtonEnabled="False" Text="{Binding DurationText, Mode=TwoWay}" IsEnabled="{Binding IsDurationEditable, Mode=OneWay}" />
                <TextBlock Text="{DynamicResource MillisecondsUnit}" FontSize="14" Margin="10,0,0,0" VerticalAlignment="Center" />
            </StackPanel>
        </ui:CardControl>

        <ui:CardControl Icon="{ui:SymbolIcon TimerOff24}" Margin="0,0,0,3">
            <ui:CardControl.Header>
                <StackPanel Orientation="Vertical">
                    <TextBlock Text="{DynamicResource MediaFlyoutAlwaysDisplay}" FontSize="14" FontWeight="Regular" />
                    <TextBlock Text="{DynamicResource MediaFlyoutAlwaysDisplayDescription}" FontSize="12" Opacity="0.5" />
                </StackPanel>
            </ui:CardControl.Header>
            <controls:ToggleSwitch IsChecked="{Binding MediaFlyoutAlwaysDisplay, Mode=TwoWay}" />
        </ui:CardControl>

        <ui:CardControl Icon="{ui:SymbolIcon AlignLeft24}" Margin="0,0,0,3">
            <ui:CardControl.Header>
                <StackPanel Orientation="Vertical">
                    <TextBlock Text="{DynamicResource CenterTitleArtistTitle}" FontSize="14" FontWeight="Regular" />
                    <TextBlock Text="{DynamicResource CenterTitleArtistDescription}" FontSize="12" Opacity="0.5" />
                </StackPanel>
            </ui:CardControl.Header>
            <controls:ToggleSwitch Name="CenterTitleArtistSwitch" IsChecked="{Binding CenterTitleArtist, Mode=TwoWay}" />
        </ui:CardControl>

        <ui:CardControl Icon="{ui:SymbolIcon ArrowRepeatAll24}" Margin="0,0,0,3">
            <ui:CardControl.Header>
                <StackPanel Orientation="Vertical">
                    <TextBlock Text="{DynamicResource RepeatButtonTitle}" FontSize="14" FontWeight="Regular" />
                    <TextBlock Text="{DynamicResource RepeatButtonDescription}" FontSize="12" Opacity="0.5" />
                </StackPanel>
            </ui:CardControl.Header>
            <controls:ToggleSwitch Name="RepeatSwitch" IsChecked="{Binding RepeatEnabled, Mode=TwoWay}" />
        </ui:CardControl>

        <ui:CardControl Icon="{ui:SymbolIcon ArrowShuffle24}" Margin="0,0,0,3">
            <ui:CardControl.Header>
                <StackPanel Orientation="Vertical">
                    <TextBlock Text="{DynamicResource ShuffleButtonTitle}" FontSize="14" FontWeight="Regular" />
                    <TextBlock Text="{DynamicResource ShuffleButtonDescription}" FontSize="12" Opacity="0.5" />
                </StackPanel>
            </ui:CardControl.Header>
            <controls:ToggleSwitch Name="ShuffleSwitch" IsChecked="{Binding ShuffleEnabled, Mode=TwoWay}" />
        </ui:CardControl>

        <ui:CardControl Icon="{ui:SymbolIcon SlideArrowRight24}" Margin="0,0,0,3">
            <ui:CardControl.Header>
                <StackPanel Orientation="Vertical">
                    <TextBlock Text="{DynamicResource ShowSeekbarTitle}" FontSize="14" FontWeight="Regular" />
                    <TextBlock Text="{DynamicResource ShowSeekbarDescription}" FontSize="12" Opacity="0.5" />
                </StackPanel>
            </ui:CardControl.Header>
            <controls:ToggleSwitch Name="SeekbarSwitch" IsChecked="{Binding SeekbarEnabled, Mode=TwoWay}" />
        </ui:CardControl>

        <ui:CardControl Icon="{ui:SymbolIcon Speaker220}" Margin="0,0,0,3">
            <ui:CardControl.Header>
                <StackPanel Orientation="Vertical">
                    <TextBlock Text="{DynamicResource ShowVolumeSliderTitle}" FontSize="14" FontWeight="Regular" />
                    <TextBlock Text="{DynamicResource ShowVolumeSliderDescription}" FontSize="12" Opacity="0.5" />
                </StackPanel>
            </ui:CardControl.Header>
            <controls:ToggleSwitch Name="VolumeSliderSwitch" IsChecked="{Binding VolumeSliderEnabled, Mode=TwoWay}" />
        </ui:CardControl>

        <ui:CardControl Icon="{ui:SymbolIcon SpeakerOff24}" Margin="0,0,0,12">
            <ui:CardControl.Header>
                <StackPanel Orientation="Vertical">
                    <TextBlock Text="{DynamicResource MediaFlyoutExcludeVolumeTitle}" FontSize="14" FontWeight="Regular" />
                    <TextBlock Text="{DynamicResource MediaFlyoutExcludeVolumeDescription}" FontSize="12" Opacity="0.5" />
                </StackPanel>
            </ui:CardControl.Header>
            <controls:ToggleSwitch Name="MediaFlyoutVolumeKeysExcludedSwitch" IsChecked="{Binding MediaFlyoutVolumeKeysExcluded, Mode=TwoWay}" />
        </ui:CardControl>
    </StackPanel>
</Page>